{
  "name": "crom-api-webhook-sample",
  "version": "1.0.0",
  "description": "Crom Services public sample: HMAC-verified webhook endpoint",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "start": "node --import tsx src/server.ts",
    "test": "node --import tsx --test test/*.test.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
